{
  "name": "adb-debloat",
  "version": "1.0.0",
  "description": "Android debloat CLI tool",
  "bin": {
    "debloat": "./debloat.js"
  },
  "scripts": {
    "build": "bun build --compile --target=bun-windows-x64 debloat.js --outfile debloat.exe"
  }
}
